Flask>=2.0
python-dotenv>=1.0
psycopg2-binary>=2.9 # Or psycopg2
Flask-Session>=0.5 # Still needed for base session logic
requests>=2.28 # For mbkauthe recaptcha/API calls
pyotp>=2.8 # For mbkauthe 2FA
bcrypt>=4.0 # For mbkauthe password hashing (even if not used in this demo)
Flask-Cors>=4.0 # For mbkauthe CORS handling
SQLAlchemy>=1.4 # For mbkauthe config/session type validation
importlib-metadata; python_version<'3.10'
Werkzeug>=2.0 # Flask dependency
Pillow # For image validation if used elsewhere