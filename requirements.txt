# requirements.txt

# Core Flask framework
Flask>=2.3.0

# For loading .env files
python-dotenv>=1.0.0

# For making HTTP requests (used in mbkauthe for reCAPTCHA, and image uploader for GitHub API)
requests>=2.28.0

# Image processing/validation (used in image uploader)
Pillow>=9.0.0

# PostgreSQL database adapter (choose one)
psycopg2-binary>=2.9.5
# OR psycopg2>=2.9.5 # If you prefer compiling

# Flask extension for server-side sessions
Flask-Session>=0.5.0

# Required if using SESSION_TYPE = "sqlalchemy"
SQLAlchemy>=1.4.0

# Password hashing library (used if EncryptedPassword=true)
bcrypt>=4.0.0

# Time-based One-Time Password library (used if MBKAUTH_TWO_FA_ENABLE=true)
pyotp>=2.8.0

# Flask extension for handling Cross-Origin Resource Sharing (good practice)
Flask-Cors>=4.0.0

# WSGI utility library (dependency of Flask, used for secure_filename)
Werkzeug>=2.3.0

# For reading package metadata (used in mbkauthe info endpoints)
# Required explicitly for Python < 3.10, included in stdlib >= 3.10
importlib-metadata>=4.4; python_version < "3.10"

# For parsing TOML files (used in mbkauthe /package-lock endpoint if parsing poetry.lock)
toml>=0.10.0